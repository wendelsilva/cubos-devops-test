<source>
  @type tail
  path /fluentd/logs/backend/*.log
  tag backend
  format none
</source>

<match **>
  @type elasticsearch
  host elasticsearch
  port 9200
  logstash_format true
  logstash_prefix docker
</match>
